<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG BUILDS ONLY: dev servers speak plain HTTP/WS on LAN/emulator.
     Release builds use the strict main config. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
